{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.7.7",
    "concurrently": "^9.0.1",
    "portfinder": "^1.0.32",
    "react": "^18.3.1",
    "react-alice-carousel": "^2.9.1",
    "react-audio-player": "^0.17.0",
    "react-dom": "^18.3.1",
    "react-icons": "^5.3.0",
    "react-router-dom": "^6.26.2",
    "react-scripts": "5.0.1",
    "react-slick": "^0.30.2",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:react": "react-scripts start",
    "start:backend": "cd servidor && npm start",
    "start": "concurrently \"npm run start:react\" \"npm run start:backend\"",
    "build:react": "react-scripts build",
    "build:backend": "cd servidor && npm run build",
    "build": "concurrently \"npm run build:react\" \"npm run build:backend\" \"serve -s build\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11"
  }
}
